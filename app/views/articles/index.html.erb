<div class="jumbotron">
<h1>Listing articles</h1>
<%= link_to 'New article', new_article_path, class: 'btn btn-outline-primary'  %>

<!-- <table>
	<tr>
		<th>Title</th>
		<th>Text</th>
		<th colspan="3"></th>
	</tr>

	<% @articles.each do |article| %>
		<tr>
			<td><%= article.title %></td>
			<td><%= article.text %></td>
			<td><%= link_to 'Show', article_path(article) %></td>
			<td><%= link_to 'Edit', edit_article_path(article) %></td>
			<td><%= link_to 'Destroy', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
		</tr>
	<% end %>
</table>

<%= link_to 'Back', root_path %> -->

<div class="container">
	<div class="row mb-2">
		<% @articles.each do |article| %>
			<div class="col-md-6">
	      <div class="card flex-md-row mb-4 box-shadow h-md-250">
	        <div class="card-body d-flex flex-column align-items-start">
	          <h3 class="mb-0">
	            <%= article.title %>
	          </h3>
	          <p class="card-text mb-auto"><%= article.text %></p>
	          <!-- <strong class="d-inline-block mb-2 text-primary">Name</strong> -->
	          <div class="buttons-line">
		          <%= link_to 'Destroy', article_path(article), class: 'btn btn-outline-secondary',
	              method: :delete,
	              data: { confirm: 'Are you sure?' } %>
		          <%= link_to 'Continue reading', article_path(article), class: 'btn btn-outline-primary'  %>
	          </div>
	          <!-- <div class="mb-1 text-muted">Nov 12</div> -->
	        </div>
	        <%= image_tag(article.image.medium.url, alt: 'Image') if article.image?  %>
	      </div>
	    </div>
		<% end %>
  </div>
</div>
</div>